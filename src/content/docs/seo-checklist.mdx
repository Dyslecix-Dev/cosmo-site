---
title: SEO checklist
description: A before-you-ship list for centralized SEO.
order: 5
updatedDate: 2026-05-05
---

import Callout from "@/components/callouts/Callout.astro";

## Before going live

- Set site in `astro.config.mjs` to your production URL

<Callout type="warning" title="SEO.astro will throw without site">
  `SEO.astro` requires site in `astro.config.mjs` because canonical URLs and absolute OG image URLs depend on it.
</Callout>

- Update `siteName` and `titleDefault` defaults in `SEO.astro`
- Optionally set `twitterCreator` in `SEO.astro` to add a global Twitter creator meta tag (or pass it per-page)
- Replace `public/placeholder-og-fallback.png` with a real OG image (1200×630 recommended) — or automate per-page cards with a generator (see <a href="/docs/recommended-integrations#og-image-generation">OG image generation</a>)
- Replace `src/assets/placeholder-home-hero.png` (and `placeholder-blog-hero.png` if you kept the blog)
- Update sitemap URL in `public/robots.txt`
- Confirm `@astrojs/sitemap` runs in your build (it's wired in `astro.config.mjs`)
- Verify each page passes a meaningful `title` and `description` to `BaseLayout`
- **Optional**: extend the auto-generated `WebSite` JSON-LD schema with page-specific schemas (`BlogPosting`) via the `<slot name="head">` in `BaseLayout`

<Callout type="note" title="Rough drafts">
  Draft entries are excluded from the sitemap and never served to users, so no `noindex` is needed. See <a href="/docs/managing-content-collections#drafting-entries">Managing content collections</a>.
</Callout>

## Per-page overrides

Pages override defaults via props on `BaseLayout`:

```astro
<BaseLayout
  title="My post"
  description="A short summary."
  image="/images/custom-og.png"
>
```

<Callout type="tip">
  Set `noindex` on draft pages or anything you don't want crawled. The canonical URL is automatically omitted when `noindex` is true.
</Callout>

## See also

- <a href="/docs/recommended-integrations#analytics">Analytics</a> — privacy-respecting traffic insight without a consent banner
- <a href="/docs/recommended-integrations#error-tracking">Error tracking</a> — find out about runtime errors before your users do
- <a href="/docs/deployment">Deployment</a> — verify OG images and the sitemap after your first deploy
